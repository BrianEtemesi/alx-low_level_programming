#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>

int _pow(int a, int b);
unsigned int binary_to_uint(const char *b);
int _putchar(char c);
void print_binary(unsigned long int n);

#endif /* MAIN_H */
